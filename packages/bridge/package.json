{
  "name": "@toyon/bridge",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/bridge.ts --outfile dist/bridge.js --minify && bun run size",
    "size": "bun scripts/size.ts"
  },
  "dependencies": {
    "@toyon/shared": "workspace:*"
  }
}
