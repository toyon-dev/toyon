{
  "name": "@orchardist/bridge",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/bridge.ts --outfile dist/bridge.js --minify"
  },
  "dependencies": {
    "@orchardist/shared": "workspace:*"
  }
}
