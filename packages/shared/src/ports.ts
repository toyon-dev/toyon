/** the daemon's default listen port; the shell dev server proxies to it */
export const DAEMON_DEFAULT_PORT = 4141;
/** the shell's Vite dev server (`bun run shell:dev`); the daemon lists it as a shell origin locally */
export const SHELL_DEV_PORT = 4142;
